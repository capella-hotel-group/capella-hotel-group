## 1. Footer Base Styles

- [x] 1.1 Set `footer` background-color to `#242f3a`, text color to white
- [x] 1.2 Remove existing max-width and padding from `.footer > div`, replace with full-width container approach

## 2. Logo Section

- [x] 2.1 Style `.footer > div:first-child` (logo section) — flex row, align-items center, gap
- [x] 2.2 Style each `p` (logo wrapper) — display flex, align-items center
- [x] 2.3 Add vertical divider between logos using `border-left` on second `p`
- [x] 2.4 Set logo `img` dimensions (width/height constraints for SVGs)

## 3. Nav Link Section

- [x] 3.1 Style `.footer > div:last-child ul` — reset list-style, margin, padding
- [x] 3.2 Style `li` and `a` — uppercase, letter-spacing, color, font-size
- [x] 3.3 Style last `li` (copyright) — smaller font-size, reduced opacity
- [x] 3.4 Remove underlines from links, set hover state

## 4. Layout — Mobile & Desktop

- [x] 4.1 Mobile default: single-column stack (logo section above nav section)
- [x] 4.2 Desktop (≥900px): two-column flex — logos left, nav right, padding 40px
- [x] 4.3 Wide (≥1440px): padding `14.427vw` both sides
